<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emjoy">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".about_us"/>
        <activity android:name=".Anime_Acivity"/>
        <activity android:name=".HomeActivity" android:theme="@style/AppCompatTheme"/>
        <activity android:name=".DIYActivity"/>
        <activity android:name=".UserActivity"/>
        <activity android:name=".SearchViewActivity" android:theme="@style/AppCompatTheme"/>

        <activity android:name=".SearchResultActvity" android:theme="@style/AppCompatTheme" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data android:name="android.app.searchable" android:resource="@xml/searchable"/>
        </activity>
        <activity android:name=".Gif_maker_Activity" />
        <activity android:name=".Crop_Activity"/>
        <activity android:name=".Filter_Activity"/>
        <activity android:name=".Doodle_Activity"/>

    </application>

</manifest>